VITE_API_BASE_URL=http://127.0.0.1:8000

# Meetings default to same-origin HTTP/WebSocket through Vite or Nginx.
# Set only for a separate API host; use https:// in production.
VITE_MEETINGS_API_BASE_URL=
